import { handleActions } from 'redux-actions';
import * as actions from 'actions';

export default handleActions({
  [actions.fetchPhoneByIdSuccess]: (state, { payload: { phone } }) => {
    return {
      currentPhone: { [phone.id]: phone },
      currentId: phone.id,
    };
  },
}, { currentPhone: {}, currentId: {} });
